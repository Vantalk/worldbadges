export function topic() {
  // let subtopic_prefix = ""
  // let room = "_0"
  // let channel = window.project_id
  //
  // if(window.room) {    room = `_${window.room}`  }
  //
  // if(window.recruitment_page) { subtopic_prefix = "ext"; room = "" }
  // else if(window.show_page) subtopic_prefix = "int"
  //
  // return `room:${subtopic_prefix}_${channel}${room}`
}

// x = window.location.href.split("/")
